# Generated from pyproject.toml — use `pip install -r requirements.txt` for
# a reproducible install. Pin versions for production; ranges shown are tested.

# --- Web framework ---
fastapi==0.111.1
uvicorn[standard]==0.29.0
httpx==0.27.0           # async test client + HTTPX transport for FastAPI

# --- Database ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# --- Validation / settings ---
pydantic==2.7.1
pydantic-settings==2.2.1
pydantic[email]==2.7.1

# --- Logging ---
structlog==24.2.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Genomics ---
cyvcf2==0.30.28

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# --- Linting / type checking (dev only) ---
ruff==0.4.7
mypy==1.10.0
types-passlib==1.7.7.20240316
